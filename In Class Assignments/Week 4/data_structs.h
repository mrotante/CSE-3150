#include <queue>
#include <stack>
#include <string>

using namespace std;

queue<int> queue_push(char filepath[]);
void queue_pop(queue<int> queue); //pop will print & throw exception
stack<int> stack_push(char filepath[]);
void stack_pop(stack<int> stack); //pop will print & throw exception